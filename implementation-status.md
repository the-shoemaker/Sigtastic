# Implementation Status

Updated: 2026-03-06

## Implemented

- Planning and tracking docs
  - Added robust execution plan in `execution-plan.md`
  - Added `AGENTS.md` handoff log for tricky findings
  - Maintained this status tracker
- Extension framework
  - Set up **WXT** project targeting Firefox (`wxt build --browser firefox`)
  - Added typed project config (`package.json`, `tsconfig.json`, `wxt.config.ts`)
- Clipboard capture integration
  - Injected page-context hook (`public/clipboard-hook.js`)
  - Intercepts both `fetch` and `XMLHttpRequest` POST calls to `/p/clipboard`
  - Captures and persists latest `value_json` + `namespace` payload
- Clipboard write reliability updates
  - Clipboard write performed in page context to `/p/clipboard`
  - Reuses captured auth/CSRF headers and full clipboard form-template fields
  - Persists request template in favorites and latest capture
  - Bootstraps template back into page hook on startup/reload
  - Raw payload first, sanitized payload fallback on failure
- Favorites data model and storage
  - Implemented typed models (`ClipboardCapture`, `Favorite`)
  - Implemented storage helpers for create/read/delete/reorder flows
  - Fixed reorder persistence bug (stored order now reflects UI order)
  - New favorites inserted at beginning of list
- Payload portability for cross-workspace reuse
  - Added payload sanitizer (`src/shared/payload.ts`)
  - Rewrites snippet `resourceId` values and applies default placement when needed
- Overlay UI and interaction stability pass
  - Darker transparent compact panel with thin light outline
  - Reduced panel/scrim blur for clearer canvas context
  - Fixed search/header area, scroll only on component list
  - Search matches both favorite names and payload content text
  - Explicit `search` vs `list` input mode model
  - Enter inserts/closes reliably
  - Enter writes favorite payload to Signavio clipboard and closes; paste remains manual (`Cmd/Ctrl+V`)
  - `Option+Delete` removes selected favorite
  - Delete selection now shifts to left neighbor
  - Fixed footer overflow by defining explicit 5-row panel grid and `minmax(0, 1fr)` list area
- Save shortcut flow
  - `Option+Shift+S` uses latest captured Signavio clipboard payload (no synthetic keyboard copy)
  - Replaced browser prompt with in-page styled modal containing two fields: `Name` and `Content`
  - `Name`/`Content` are saved as visual-only metadata and do not alter pasted payload
  - Visual metadata now tracks custom-vs-default state so improved autogenerated names still apply when not customized
- Preview rendering
  - Expanded BPMN icon coverage (task/subprocess/gateway variants/event variants/flows/data/pool-lane/annotation/message)
  - Simplified large icon model: big yellow shape reflects BPMN element only
  - Added chained top-right context badges with multi-element indicator
  - Added full-width bottom type label and center type badge for task variants
  - Bottom card text now shows only content text (or `Empty`)
  - Removed warning/error/triangle-style context badges
  - Removed secondary yellow preview background for task-like cards
  - Increased card/icon size and added SVG viewBox safety margin to prevent clipped outlines
  - Multi-element previews now render up to 3 overlapped mini icons in bubble layout
  - Type badge rendering remains independent from bubble stack layout (kept centered)
  - Added duplicate badge when favorites share identical visual signature (`type`, `name`, `content`, `other badges`)
  - Content text area now supports 3 lines with bottom fade-out mask
  - Improved `Data Store` and `Text Annotation` icon detail
  - Element type labels now use middle-ellipsis truncation (Finder-style) for better disambiguation
  - Adjusted type label truncation to strict middle `...` only (removed CSS end-ellipsis clipping)
  - Re-centered compensation/back-arrow event glyph and restored centered task type badges
  - Widened preview icon background to increase label space
  - Save modal now uses responsive + border-box input sizing to avoid overflow
  - Duplicate badge icon uses stacked-copy style (per latest UX preference)
  - Multi-element bubble icon stacks made smaller and more spread out
  - Type name shortening now uses actual available label width (dynamic middle-ellipsis fit)
  - Card content text alignment set to top within the label area
  - Two-element bubble stacks are centered as a grouped stagger (three-element layout unchanged)
  - Gateway/diamond icons now render at 75% height for improved proportion
  - Small-screen overflow handling tightened via media-query-only layout tuning (`<=1000px`, `<=700px`)
  - Multi-element save default naming now follows `{first element name}, more...`
  - Arrow-key navigation now auto-scrolls selected card into view at top with 10px padding
  - Small-screen cards now size content area dynamically (media-query-only: no fixed card height)
  - Reordering now also follows selection with the same out-of-view top+10px smooth scroll behavior
  - Expanded icon variants for task subtypes and event definition flavors (start/intermediate/end/boundary)
- Annotation carry-over
  - Favorites now attempt to include linked text annotations + association connectors from payload `linked` metadata at save/sanitize time

## Validation Run (Local)

- `npm install` ✅
- `npm run typecheck` ✅
- `npm run build` ✅ (Firefox output generated in `.output/firefox-mv2`)

## Next

1. Manual Signavio verification with latest build
   - Confirm persisted favorites still insert after extension reload/restart
   - Confirm no 401 on `/p/clipboard` write after restart
2. Verify icon edge cases
   - Additional BPMN stencils not yet mapped use fallback icon
3. Optional enhancement
   - Auto-paste mode toggle after successful clipboard write
